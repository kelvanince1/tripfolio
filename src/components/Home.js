// Modules
import React, {Component} from 'react';

// Components

// Styles and images

class Home extends Component {
  render() {
    return(
      <main>
        <h1>Travel Planner</h1>
        <p>Login to begin!</p>
      </main>
    );
  }
}

export default Home;
